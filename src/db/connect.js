// src/db/connect.js
// Temporary in-memory mode — MongoDB fix coming later

const connectDB = async () => {
  console.log('🧠 Aura running in memory mode — DB coming soon')
}

export default connectDB